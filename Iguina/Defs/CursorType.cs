namespace Iguina.Defs
{
    /// <summary>
    /// Predefined cursor categories the UISystem can render. Map to the
    /// CursorDefault / CursorInteractable / CursorDisabled / CursorLocked /
    /// CursorIBeam fields on <see cref="SystemStyleSheet"/>.
    /// </summary>
    public enum CursorType
    {
        Default,
        Interactable,
        Disabled,
        Locked,
        IBeam,
    }
}
